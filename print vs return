تفاوت print() با return

• print یک تابع است که برای نمایش خروجی به کاربر در کنسول یا ترمینال استفاده می‌شود
• این تابع مقداری را که به آن داده می‌شود به صورت متنی چاپ می‌کند و هیچ مقداری را به تابع فراخوانی کننده برنمی‌گرداند

 return یک کلیدواژه است که برای بازگرداندن مقدار از یک تابع استفاده می‌شود
وقتی که تابع با استفاده از ری ترن خاتمه می‌یابد مقدار مشخص شده به تابع فراخوانی کننده برمی‌گردد و می‌توان از آن در سایر بخش‌های برنامه استفاده کرد


پس نتیجه می گیریم که 

 از print برای نمایش خروجی به کاربر استفاده می‌شود، در حالی که از return برای بازگرداندن مقدار از یک تابع به کار می‌رود
